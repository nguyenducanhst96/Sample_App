<%= form_for find_followed(@user.id),
  html: { method: :delete }, remote: true do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit t(".submit_unfollow"), class: "btn" %>
<% end %>
