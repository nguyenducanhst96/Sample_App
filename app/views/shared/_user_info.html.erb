<%= link_to gravatar_for(current_user, size: Settings.UserInfo.size_picture),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".content_url_profile"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t(".micropost") %></span>
